# Shared agent skills

This directory contains agent-neutral workflow definitions that are not tied to Claude-specific wrappers.

Use these docs as the primary skill contract for cross-agent integrations.

Current shared skills:

- `skills/resume/SKILL.md` — shared resume workflow definition built on the public `resume_runtime/` entrypoints
